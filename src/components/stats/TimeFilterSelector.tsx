
import React from 'react';

// Ce composant n'est plus utilisé, mais nous le gardons comme un composant vide
// pour éviter les erreurs de compilation, car il est peut-être importé ailleurs
const TimeFilterSelector: React.FC<any> = () => {
  return null;
};

export default TimeFilterSelector;
